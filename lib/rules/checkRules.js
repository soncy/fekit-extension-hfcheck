// Generated by CoffeeScript 1.9.0
(function() {
  var getSuffix, header, path, rules;

  path = require('path');

  header = require('./header');

  rules = [header];

  exports.applyRules = function(filePath) {
    var fileName, suffix;
    fileName = path.basename(filePath);
    suffix = getSuffix(fileName);
    if (suffix === 'css') {
      return rules.forEach(function(rule) {
        var ret;
        ret = rule.check(filePath);
        if (ret.length !== 0) {
          return console.log("\n\n " + filePath + " 检测不通过， 原因：\n" + (ret.join()) + " \n\n");
        }
      });
    }
  };

  getSuffix = function(fileName) {
    var arr, len;
    arr = fileName.split('.');
    len = arr.length;
    if (len === 1) {
      return null;
    }
    return arr[len - 1];
  };

}).call(this);
